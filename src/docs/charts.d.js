/**
 * @typedef {FuturePieChartDataItem[]} FuturePieChartData
 */

/**
 * @typedef {object} FuturePieChartDataItem
 * @property {number} value - The item value.
 * @property {string} label - The item label.
 * @property {string} color - The item color.
 */

/**
 * @typedef {PieChartDataItem[]} PieChartData
 */

/**
 * @typedef {object} PieChartDataItem
 * @property {number} id - The item ID.
 * @property {number} value - The item value.
 * @property {string} label - The item label.
 * @property {string} color - The item color.
 */

export default {};
